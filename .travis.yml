language: node_js
cache:
  directories:
   - node_modules
before_script:
 - npm run compile
env:
 - CXX=g++-4.8
addons:
  apt:
    sources:
     - ubuntu-toolchain-r-test
    packages:
     - g++-4.8
